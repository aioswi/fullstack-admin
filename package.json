{
  "name": "with-vue-nuxt",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@aioswi/tsconfig": "workspace:*",
    "eslint": "^8.57.0",
    "turbo": "latest"
  }
}
